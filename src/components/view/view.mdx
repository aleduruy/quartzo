---
name: View
menu: Atoms
route: /component-view 
---
import { Playground, PropsTable } from 'docz';
import { View, Text } from '..';

# View
The **View** components is a generic wrapper that can receive any kind of children. On the web environment it can render a couple of different HTML tags, wich will keep the page semantics.

### Semantics
Possible tags to be used:
- `section`
- `article`
- `form`
- `aside`
- `div`

### Playground
<Playground>
  <View tag="section">Hello! I'm wrapped in a View, and my tag is a section.</View>
</Playground>

### Tabela de Props
<PropsTable of={View} />